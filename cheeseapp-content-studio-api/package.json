{
  "name": "cheeseapp-content-studio-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Authenticated API boundary for the internal Cheese Content Studio",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "find src -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "devDependencies": {
    "wrangler": "^4.10.0"
  }
}
